---
import BaseLayout from '../layouts/BaseLayout.astro';
import SiteHeader from '../components/SiteHeader.astro';
import SiteFooter from '../components/SiteFooter.astro';
import { getCollection } from 'astro:content';

const projects = (await getCollection('projects')).sort((a, b) => a.data.order - b.data.order);
const articles = (await getCollection('writing')).sort((a, b) => a.data.order - b.data.order);
const featuredProject = projects.find((project) => project.data.featured) ?? projects[0];

const personJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: 'Musa Ibrahim',
  jobTitle: 'Software Engineering Student and Front-end Developer',
  url: 'https://musa-ibrahim-lac.vercel.app/',
  sameAs: [
    'https://github.com/Zubish',
    'https://www.linkedin.com/in/realvjy',
    'https://x.com/goastwriterspen?t=9sXImSmsilFYY_x90LNbrQ&s=09'
  ]
};
---
<BaseLayout>
  <script is:inline slot="head" type="application/ld+json" set:html={JSON.stringify(personJsonLd)}></script>
  <SiteHeader />

  <main id="main-content">
    <section id="top" class="hero">
      <div class="hero-backdrop" aria-hidden="true"></div>
      <div class="container hero-grid">
        <div class="hero-copy reveal">
          <p class="eyebrow">400-level software engineering student / Pharmacy Technician / AI-assisted product builder</p>
          <h1>I build practical product interfaces for workflows that need structure, speed, and trust.</h1>
          <p class="hero-summary">I'm a 400-level Software Engineering student at Miva Open University and a Pharmacy Technician. I use AI-assisted development and prompt engineering to turn product ideas into working front-end experiences, with a strong interest in HealthTech, pharmacy workflow, and pharmacy-integrated systems.</p>
          <div class="hero-cta-row">
            <a class="button button-primary" href="#work">Explore my work</a>
            <a class="button button-secondary" href="#contact">Start a conversation</a>
            <a class="button button-ghost" href="mailto:musabish98@gmail.com?subject=Resume%20Request">Request my resume</a>
          </div>
          <ul class="hero-highlights">
            <li>HealthTech and pharmacy operations UX</li>
            <li>AI-assisted front-end development</li>
            <li>Prompt engineering for product and code workflows</li>
          </ul>
        </div>

        <aside class="hero-panel reveal">
          <div class="hero-card hero-card-profile">
            <img src="/img/Professional%20photo%20me.jpg" alt="Portrait of Musa Ibrahim" class="profile-photo" loading="eager" decoding="async" width="220" height="220">
            <div>
              <p class="card-label">Currently focused on</p>
              <p class="card-strong">HealthTech tools, pharmacy workflow systems, and front-end products that make complex actions easier to complete.</p>
            </div>
          </div>
          <div class="hero-card hero-card-metric">
            <p class="card-label">Inside this portfolio</p>
            <div class="metric-grid">
              <div>
                <span class="metric-number">{projects.length}</span>
                <span class="metric-label">real product builds</span>
              </div>
              <div>
                <span class="metric-number">{articles.length}</span>
                <span class="metric-label">working notes</span>
              </div>
              <div>
                <span class="metric-number">1</span>
                <span class="metric-label">HealthTech focus</span>
              </div>
              <div>
                <span class="metric-number">AI</span>
                <span class="metric-label">assisted workflow</span>
              </div>
            </div>
          </div>
        </aside>
      </div>
    </section>

    <section class="featured-band reveal">
      <div class="container featured-band-inner">
        <p class="featured-band-title">Featured HealthTech build</p>
        <p class="featured-band-copy">RxLedger connects my pharmacy background with my software engineering work: branch inventory, expiry-aware stock control, POS flow, staff roles, and audit-ready pharmacy records.</p>
      </div>
    </section>

    <section id="work" class="section container reveal">
      <div class="section-heading">
        <p class="eyebrow">Selected work</p>
        <h2>Three real builds shaped around workflow, roles, and decision points</h2>
        <p>RxLedger, BlackCrow, and VisionEcho show how I approach interfaces where users have to track stock, protect transactions, verify reports, or move through multi-step product flows without losing context.</p>
      </div>

      {featuredProject && (
        <article class="featured-project">
          <button
            class="featured-project-media image-modal-trigger"
            type="button"
            data-modal-image={featuredProject.data.image}
            data-modal-alt={featuredProject.data.alt}
            aria-label={`Open ${featuredProject.data.title} screenshot`}
          >
            <img src={featuredProject.data.image} alt={featuredProject.data.alt} loading="lazy" decoding="async" width="900" height="620">
          </button>
          <div class="featured-project-copy">
            <p class="eyebrow">{featuredProject.data.eyebrow}</p>
            <h3>{featuredProject.data.title}</h3>
            <p>{featuredProject.data.description}</p>
            <ul class="tag-list">
              {featuredProject.data.tags.map((tag) => <li>{tag}</li>)}
            </ul>
            <div class="action-row">
              <a class="button button-primary" href={`/projects/${featuredProject.id}`}>Read case study</a>
            </div>
          </div>
        </article>
      )}

      <div class="projects-grid">
        {projects.map((project) => (
          <article class="project-card">
            <button
              class="project-thumbnail-button image-modal-trigger"
              type="button"
              data-modal-image={project.data.image}
              data-modal-alt={project.data.alt}
              aria-label={`Open ${project.data.title} screenshot`}
            >
              <img class="project-thumbnail" src={project.data.image} alt={project.data.alt} loading="lazy" decoding="async" width="640" height="420">
            </button>
            <div class="project-card-body">
              <p class="project-type">{project.data.type}</p>
              <h3>{project.data.title}</h3>
              <p>{project.data.cardDescription}</p>
              <ul class="tag-list">
                {project.data.cardTags.map((tag) => <li>{tag}</li>)}
              </ul>
              <div class="card-links">
                <a href={`/projects/${project.id}`}>Case study</a>
              </div>
            </div>
          </article>
        ))}
      </div>
    </section>

    <section id="about" class="section container reveal">
      <div class="split-panel">
        <div>
          <p class="eyebrow">About me</p>
          <h2>I build from two worlds: software engineering and pharmacy operations.</h2>
          <p>I'm a 400-level Software Engineering student at Miva Open University, and I also work from the practical eye of a Pharmacy Technician. That background makes me notice inventory pressure, expiry dates, handover gaps, staff roles, and the small interface decisions that can slow down a real counter workflow.</p>
          <p>I use AI as a coding assistant, product thinking partner, and prompt engineering tool to move faster without skipping judgment. My strongest interest is HealthTech, especially pharmacy workflow, pharmacy-integrated systems, and operational products where the interface has to support real daily work.</p>
        </div>
        <div class="about-callout">
          <p class="callout-title">Positioning headline</p>
          <p>Software engineering student and AI-assisted front-end builder focused on HealthTech, pharmacy workflow, and role-aware product interfaces.</p>
          <p class="callout-title">Currently open to</p>
          <p>Internships, HealthTech collaborations, pharmacy software projects, front-end roles, and product teams that value domain-aware builders.</p>
        </div>
      </div>
    </section>

    <section id="skills" class="section container reveal">
      <div class="section-heading">
        <p class="eyebrow">Skills and tools</p>
        <h2>The stack I use to turn domain problems into usable screens</h2>
      </div>
      <div class="skills-grid">
        <article class="info-card">
          <h3>Front-end implementation</h3>
          <ul class="plain-list">
            <li>Semantic HTML</li>
            <li>Responsive CSS layouts</li>
            <li>Vanilla JavaScript interactions</li>
            <li>Accessibility-minded structure</li>
          </ul>
        </article>
        <article class="info-card">
          <h3>Product and domain thinking</h3>
          <ul class="plain-list">
            <li>Pharmacy workflow mapping</li>
            <li>Inventory and expiry logic</li>
            <li>Role-aware user journeys</li>
            <li>Operational dashboard planning</li>
          </ul>
        </article>
        <article class="info-card">
          <h3>AI-assisted workflow</h3>
          <ul class="plain-list">
            <li>Prompt engineering</li>
            <li>AI-assisted code iteration</li>
            <li>Interface critique and refinement</li>
            <li>Testing ideas against real user flows</li>
          </ul>
        </article>
      </div>
    </section>

    <section class="section container reveal">
      <div class="section-heading">
        <p class="eyebrow">What I bring</p>
        <h2>A builder profile shaped by code, prompts, and pharmacy context</h2>
      </div>
      <div class="trust-grid">
        <article class="info-card">
          <h3>Domain awareness</h3>
          <p>My pharmacy background helps me design around branch stock, expiry risk, POS handoff, staff permissions, and the pressure of fast service environments.</p>
        </article>
        <article class="info-card">
          <h3>Prompt engineering</h3>
          <p>I use prompts to clarify product flows, compare interface options, generate implementation paths, and review code decisions before they reach the screen.</p>
        </article>
        <article class="info-card">
          <h3>Recruiter-ready proof</h3>
          <p>The portfolio links to live projects and repositories, so recruiters can inspect the product, read the case study, and review the implementation trail.</p>
        </article>
      </div>
    </section>

    <section class="section container reveal" aria-labelledby="testimonials-title">
      <div class="section-heading">
        <p class="eyebrow">Collaboration fit</p>
        <h2 id="testimonials-title">How I show up on a product team</h2>
        <p>I work best where product details matter: the screen has a job, the data has rules, and the user needs fewer guesses between one action and the next.</p>
      </div>
      <div class="testimonials-grid">
        <article class="quote-card">
          <h3>Clear product reasoning</h3>
          <p>I explain interface decisions through the user task: what a cashier, pharmacist, buyer, seller, reporter, editor, or recruiter needs to understand next.</p>
        </article>
        <article class="quote-card">
          <h3>Fast, careful iteration</h3>
          <p>I use AI to move quickly through options, then slow down on the parts that need human judgment: wording, hierarchy, edge cases, mobile behavior, and accessibility.</p>
        </article>
      </div>
    </section>

    <section id="process" class="section container reveal">
      <div class="section-heading">
        <p class="eyebrow">How I work</p>
        <h2>My process starts with the workflow, then moves into the interface</h2>
      </div>
      <div class="process-grid">
        <article class="process-step">
          <span class="step-number">01</span>
          <h3>Map the real task</h3>
          <p>I identify the roles, data, risks, and repeated actions first, whether the product is handling stock, escrow funds, civic reports, or recruiter review.</p>
        </article>
        <article class="process-step">
          <span class="step-number">02</span>
          <h3>Prompt, compare, build</h3>
          <p>I use prompt engineering to explore flows and implementation paths, then translate the strongest direction into responsive front-end screens.</p>
        </article>
        <article class="process-step">
          <span class="step-number">03</span>
          <h3>Test the handoff points</h3>
          <p>I check the moments where users can get confused: state changes, empty data, mobile stacking, form errors, next actions, and links to the live product or repo.</p>
        </article>
      </div>
    </section>

    <section id="writing" class="section container reveal">
      <div class="section-heading">
        <p class="eyebrow">Writing</p>
        <h2>Working notes from building and refining product interfaces</h2>
        <p>These notes connect my portfolio decisions to the problems I keep meeting in real builds: mobile order, accessibility, specific copy, and workflows that need less friction.</p>
      </div>
      <div class="writing-grid">
        {articles.map((article) => (
          <article class="writing-card">
            <p class="writing-type">{article.data.type}</p>
            <h3><a href={`/writing/${article.id}`}>{article.data.title}</a></h3>
            <p>{article.data.description}</p>
          </article>
        ))}
      </div>
    </section>

    <section id="contact" class="section container reveal">
      <div class="contact-shell">
        <div class="contact-copy">
          <p class="eyebrow">Contact</p>
          <h2>Have a HealthTech, front-end, or product workflow opportunity?</h2>
          <p>I'm open to internships, collaborations, freelance builds, and product teams where my software engineering training, pharmacy background, and AI-assisted workflow can be useful.</p>
          <div class="contact-links">
            <a href="mailto:musabish98@gmail.com">musabish98@gmail.com</a>
            <a href="https://github.com/Zubish" target="_blank" rel="noopener noreferrer">GitHub</a>
            <a href="https://www.linkedin.com/in/realvjy" target="_blank" rel="noopener noreferrer">LinkedIn</a>
            <a href="https://x.com/goastwriterspen?t=9sXImSmsilFYY_x90LNbrQ&s=09" target="_blank" rel="noopener noreferrer">X</a>
          </div>
        </div>

        <div class="contact-form-card">
          <div id="contact-status" class="contact-status" aria-live="polite"></div>
          <form id="contact-form" novalidate>
            <label for="contact-name">Name</label>
            <input id="contact-name" name="name" type="text" required aria-required="true" autocomplete="name">
            <p class="field-error" id="contact-name-error"></p>

            <label for="contact-email">Email</label>
            <input id="contact-email" name="email" type="email" required aria-required="true" autocomplete="email">
            <p class="field-error" id="contact-email-error"></p>

            <label for="contact-message">Message</label>
            <textarea id="contact-message" name="message" rows="5" required aria-required="true"></textarea>
            <p class="field-error" id="contact-message-error"></p>

            <button type="submit" class="button button-primary">Open email draft</button>
          </form>
        </div>
      </div>
    </section>
  </main>

  <SiteFooter />
</BaseLayout>
